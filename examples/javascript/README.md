# JavaScript Examples

- **app.js** — Two OpenAI calls: one using GPT-4 (expensive), one using GPT-4o-mini (optimized). Inferency will recommend switching the GPT-4 call to a cheaper model.

## Try It

```bash
inferency scan examples/javascript
inferency report examples/javascript
```
